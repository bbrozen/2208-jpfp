const initState = {
    data: [
        {
            id: 1,
            name: "Brendan",
            type: "undergrad"
        }
    ]
}

const studentReducer = (state = initState, action) => {
    return state;
}

export default studentReducer